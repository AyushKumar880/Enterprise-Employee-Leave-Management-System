package com.algoworks.leavemanagement.entity;

/**
 * Enum representing valid leave request statuses.
 * Restricts values strictly to PENDING, APPROVED, or REJECTED.
 */
public enum LeaveStatus {
    PENDING,
    APPROVED,
    REJECTED
}
